[project]
name = "anders"
version = "0.1.0"
description = "File-backed personal assistant briefing tools"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
anders = "anders.cli:main"

[tool.ruff]
line-length = 100
